const calculateDogAge= function(age) {
    let dogYears = 7 * age;
    console.log("Your doggie is " + dogYears + " years old in dog years!");
}

calculateDogAge(1);
calculateDogAge(4);
calculateDogAge(10);